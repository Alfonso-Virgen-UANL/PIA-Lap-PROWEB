$(function crearTarjeta(registro) {
    $("#btn1").click(function() {
        if (confirm("¿Estás seguro de que quieres eliminar esta reseña?")) {
            $("#tarjeta1").hide(300);
        }
    });
    $("#btn2").click(function() {
        if (confirm("¿Estás seguro de que quieres eliminar esta reseña?")) {
            $("#tarjeta2").hide(300);
        }
    });
    $("#btn3").click(function() {
        if (confirm("¿Estás seguro de que quieres eliminar esta reseña?")) {
            $("#tarjeta3").hide(300);
        }
    });
    $("#btn4").click(function() {
        if (confirm("¿Estás seguro de que quieres eliminar esta reseña?")) {
            $("#tarjeta4").hide(300);
        }
    });
    $("#btn5").click(function() {
        if (confirm("¿Estás seguro de que quieres eliminar esta reseña?")) {
            $("#tarjeta5").hide(300);
        }
    });
});
